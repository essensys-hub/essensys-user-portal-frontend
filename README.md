# essensys-user-portal-frontend

SPA domotique Essensys — `/portal/*` sur `https://mon.essensys.fr`.

## Dev

```bash
npm install
npm run dev
```

JWT : stocker `essensys_token` dans localStorage (émis par support-site après OAuth).

## Licence

MIT
